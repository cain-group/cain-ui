{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build --stream",
    "release":"lerna publish --no-commit-hooks --dist-tag latest",
    "bootstrap": "lerna bootstrap"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "turbo": "^1.2.5"
  }
}
